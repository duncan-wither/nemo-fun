#!/bin/sh
# Following Line is to make sure you configure action.
notify-send "Configure Server to Send" "This nemo action (move2serverSSH) needs to be configured."

# This is the code, Uncomment when configures (addresses etc.)
#scp "$1" username@serverIPaddress:/home/username/
#notify-send "Video Sent" "This video has finished sending:\n$1"
